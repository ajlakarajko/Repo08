// Only change code below this line
function convertToInt(str) { 
   return parseInt(str);

    // return convertStrToInt;
}

console.log(convertToInt("BA")); // res will be modified but returned value is undefined
// Only change code above this line
module.exports = convertToInt;

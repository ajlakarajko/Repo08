// Only change code below this line
function convertStrToInt(str) { 
    var convertStrToInt = parseInt(str);

    return convertStrToInt;
}

console.log(convertStrToInt(44)); // res will be modified but returned value is undefined
// Only change code above this line
module.exports = convertStrToInt;
